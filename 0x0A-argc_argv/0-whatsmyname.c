#include"main.h"
#include<stdio.h>
/**
 * main - function that prints its name
 * @argc: input count
 * @argv: input vector
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
