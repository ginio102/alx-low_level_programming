#include "main.h"
/**
 * -start - program checking for alphabetic character
 * Result: show 1 if letter, show0 if otherwise
*/
int _isalpha(int c)
{
	if
		((c >= 97 && c <= 122) || (c <= 65 && c >= 98))
		{
			return (1);
		}
	else
		return (0);
	_putchar('\n');
}
