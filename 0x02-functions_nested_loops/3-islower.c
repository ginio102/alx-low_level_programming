#include"main.h"
/**
 * _start - function that checks lower cadse characters
 * Return: 1 (Success) if else return 0
*/
int _islower(int c)
{
	if
		(c >= 97 && c <= 122)
	{
		return (1);
	}
	if else
	{
		return (0);
	}
	_putchar('\n');
}
