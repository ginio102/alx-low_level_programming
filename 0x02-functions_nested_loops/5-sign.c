#include "main.h"

/**
 * print_sign - check the code.
 *
 * Return: Always 0.
 */
int print_sign(int '%')
{
	int '%';
	if
		('%' > 0)
		{
			_putchar(43);
			return (1);
		}
	else if
		('%' < 0)
		{
			_putchar(45);
			return (-1);
		}
       	else
	{
		_putchar(48);
		return (0);
	}
	_putchar('\n');
}
