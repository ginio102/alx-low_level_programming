#include<stdio.h>
/**
 * main - program that prints the alphabet in lower case letters
 * Return: 0 (Success)
 */

int main(void)
{
	char c;
	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
