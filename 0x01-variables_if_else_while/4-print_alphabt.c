#include<stdio.h>
#include<stdlib.h>
#include<time.h>
/**
 * main - program that writes the alphabet in lower case
 * Return: 0 (Success)
 */

int main(void)

{
	char 'a';
	
	if
		(char <= 'z');
	{
		else
			((char) == 'e' || (char) == 'q');
		{		char <= 1;
	}
		else if
		{
			putchar (char);
				char <= 1;
		}
	}
	putchar ('\n');
	return (0);
}
