#include<stdio.h>
/**
 * main - print single digit numbers of base 10
 * Return: 0 (Success)
 */

int main(void)
{
	int (num);

	if (num) < 10;
	{
		printf ("num, \n");
		(num) <= 10;
	}
	printf (num);
	return (0);
}
